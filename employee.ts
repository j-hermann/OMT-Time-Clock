// Should be modified to represent what is entered in database
export class Employee{
  id: number;
  name: string;
  active: boolean;
  // may have to add clockinTime/clockoutTime as something bound to employee class or could do during api integration
  // clockinTime: string;
  // clockoutTime: string;
}
